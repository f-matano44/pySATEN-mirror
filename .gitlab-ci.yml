image: python:3.12-slim

stages:
  - lint
  - build
  - deploy

variables:
  PIP_ROOT_USER_ACTION: "ignore"

lint:
  stage: lint
  rules:
    - when: always
  script:
    - python3 --version
    - python3 -m pip install -U black isort flake8 mypy numpy
    - black --check src/pysaten/
    - isort --check-only src/pysaten/
    - flake8 src/pysaten/
    - mypy src/pysaten/

build:
  stage: build
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  dependencies:
    - lint
  script:
    - python3 -m pip install -U build
    - python3 -m build
  artifacts:
    paths:
      - dist/

deploy:  # https://docs.pypi.org/trusted-publishers/using-a-publisher/#gitlab-cicd
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
  dependencies:
    - build
  id_tokens:
    PYPI_ID_TOKEN:
      aud: pypi
  script:
    - python3 -m pip install -U twine
    - twine --no-color upload dist/* --disable-progress-bar
